{
  "name": "celura",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev:api": "npm -w api run dev",
    "dev:dashboard": "npm -w dashboard run dev",
    "build:api": "npm -w api run build",
    "build:dashboard": "npm -w dashboard run build",
    "db:push": "npm -w db run push",
    "typecheck": "npm -w api run typecheck && npm -w dashboard run typecheck"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
